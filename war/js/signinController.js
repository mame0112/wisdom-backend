wisdomApp.controller('SigninController', ['$scope', '$routeParams', 'log', function($scope, $routeParams, log){
 	log.d("SigninController");
 	// $scope.userId = $routeParams.userId;
 	// log.d("userId: " + $scope.userId);
}]);
